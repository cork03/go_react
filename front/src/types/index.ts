export type Company = {
  name: string
  postalCode: string
  prefecture: string
  town: string
  area: string
  tel: string
}

export type User = {
  name: string
  email: string
  password: string
}
